name: saengil_alrim
description: "A Flutter application for managing birthdays and anniversaries, providing reminders and notifications to help users remember important dates and celebrate with their loved ones."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.11.5

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  shared_preferences: ^2.2.3
  intl: ^0.20.2
  flutter_local_notifications: ^21.0.0
  timezone: ^0.11.0
  flutter_timezone: ^5.1.0
  image_picker: ^1.1.2
  file_picker: ^8.1.4
  audioplayers: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4

flutter:
  uses-material-design: true

  assets:
    - assets/icon/saengil_alrim_logo.png
    - assets/sticker/

  fonts:
    - family: Comfortaa
      fonts:
        - asset: assets/fonts/Comfortaa/static/Comfortaa-Regular.ttf
        - asset: assets/fonts/Comfortaa/static/Comfortaa-Light.ttf
          weight: 300
        - asset: assets/fonts/Comfortaa/static/Comfortaa-Medium.ttf
          weight: 500
        - asset: assets/fonts/Comfortaa/static/Comfortaa-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/Comfortaa/static/Comfortaa-Bold.ttf
          weight: 700

    - family: GloriaHallelujah
      fonts:
        - asset: assets/fonts/gloria_hallelujah/GloriaHallelujah-Regular.ttf

    - family: PlaywriteUSModern
      fonts:
        - asset: assets/fonts/Playwrite_US_Modern/static/PlaywriteUSModern-Regular.ttf
        - asset: assets/fonts/Playwrite_US_Modern/static/PlaywriteUSModern-Light.ttf
          weight: 300
        - asset: assets/fonts/Playwrite_US_Modern/static/PlaywriteUSModern-ExtraLight.ttf
          weight: 200
        - asset: assets/fonts/Playwrite_US_Modern/static/PlaywriteUSModern-Thin.ttf
          weight: 100

flutter_launcher_icons:
  android: "launcher_icon"
  ios: false
  image_path: "assets/icon/saengil_alrim_logo.png"
  min_sdk_android: 21

